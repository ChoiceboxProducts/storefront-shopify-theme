{% if section.settings.svg_content != blank %}
  <style>
    .svg-{{ section.id }} {
      width: calc({{ section.settings.width }}px * {{ section.settings.scale }});
      height: calc({{ section.settings.height }}px * {{ section.settings.scale }});
      fill: {{ section.settings.fill_color }}
    }

    .svg-{{ section.id }} .accent {
      fill: {{ section.settings.accent_color }}
    }
  </style>

  <svg class="svg-{{ section.id }}" xmlns="http://www.w3.org/2000/svg" width="{{ section.settings.width }}" height="{{ section.settings.height }}" viewbox="0 0 {{ section.settings.width }} {{ section.settings.height}}">
    {{ section.settings.svg_content }}
  </svg>
{% endif %}

{% schema %}
  {
    "name": "Choicebox Logo",
    "settings": [
      {
        "type": "html",
        "id": "svg_content",
        "label": "SVG Contents"
      },
      {
        "type": "color",
        "id": "fill_color",
        "label": "Fill Color",
        "default": "#000"
      },
      {
        "type": "color",
        "id": "accent_color",
        "label": "Accent Color",
        "default": "#000"
      },
      {
        "type": "text",
        "id": "width",
        "label": "Intrinsic Width",
        "default": "200"
      },
      {
        "type": "text",
        "id": "height",
        "label": "Intristic Height",
        "default": "200"
      },
      {
        "type": "text",
        "id": "scale",
        "label": "Scale",
        "default": "1"
      }
    ],
    "presets": [{
      "name": "Choicebox Logo",
      "category": "Images"
    }]
  }
{% endschema %}